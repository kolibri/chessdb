---
- hosts: all
  pre_tasks:
    - name: ensure apt-transport-https
      become: true
      apt:
        name: apt-transport-https
        update_cache: true
        state: present
  tasks:
    - debug: var=hostvars
  roles:
    - role: apps/chessdb
      become: true
