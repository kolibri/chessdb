{% extends 'baseListByResult.html.twig' %}

{% block title %}{{ 'security.profile.title'|trans }}{% endblock %}

{% block content %}
    {{ form_start(form) }}
    <fieldset>
        {{ form_rest(form) }}
        <button type="submit">{{ 'security.profile.form.submit'|trans }}</button>
    </fieldset>
    {{ form_end(form) }}

    {{ parent() }}
{% endblock %}
