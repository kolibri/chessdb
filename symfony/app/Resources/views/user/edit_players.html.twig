{% extends 'base.html.twig' %}

{% block body %}
    <div class="user">
        <h1>Edit Players</h1>
        {{ dump(user) }}
{#
        {{ form_start(form) }}
        {{ form_rest(form) }}
        <button type="submit">Register</button>
        {{ form_end(form) }}
#}
    </div>
{% endblock %}