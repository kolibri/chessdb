{% extends 'base.html.twig' %}

{% block title %}{{ 'leaderboard.show.title'|trans }}{% endblock %}

{% block content %}
    <table id="leaderboard">
        <thead>
        <tr>
            <th>Player</th>
            <th>Games</th>
            <th>Won</th>
            <th>Lost</th>
            <th>Draw</th>
        </tr>
        </thead>
        <tbody>
    {% for player in leaderboard.items %}
        <tr>
            <td>{{ player.player }}</td>
            <td>{{ player.games }}</td>
            <td>{{ player.won }}</td>
            <td>{{ player.lost }}</td>
            <td>{{ player.draw }}</td>
        </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
