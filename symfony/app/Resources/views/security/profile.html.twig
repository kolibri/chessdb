{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ 'security.profile.title'|trans }}</h1>

    {{ form_start(form) }}
    <fieldset>
        {{ form_rest(form) }}
        <button type="submit">{{ 'security.profile.form.submit'|trans }}</button>
    </fieldset>
    {{ form_end(form) }}

{% endblock %}
